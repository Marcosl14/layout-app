export enum PositionTypesEnum {
    static = 'static',
    relative = 'relative',
    absolute = 'absolute',
    fixed = 'fixed',
    sticky = 'sticky',
    inherit = 'inherit',
    initial = 'initial',
    revert = 'revert',
    'revert-layer' = 'revert-layer',
    unset = 'unset',
    none = 'none',
}
