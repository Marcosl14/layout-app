export enum DisplayTypesEnum {
    flex = 'flex',
    'inline-flex' = 'inline-flex',
    grid = 'grid',
    'inline-grid' = 'inline-grid',
    block = 'block',
    inline = 'inline',
    contents = 'contents',
    inherit = 'inherit',
    initial = 'initial',
    revert = 'revert',
    unset = 'unset',
    none = ''
}
