export enum AlignFlexSelfEnum {
    auto = 'auto',
    'flex-start' = 'flex-start',
    'flex-end' = 'flex-end',
    center = 'center',
    'baseline' = 'baseline',
    stretch = 'stretch',
    none = '',
}
