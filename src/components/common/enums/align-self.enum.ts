export enum AlignSelfEnum {
    auto = 'auto',
    'flex-start' = 'flex-start',
    'flex-end' = 'flex-end',
    center = 'center',
    'baseline' = 'baseline',
    stretch = 'stretch',
    none = '',
}
